{
  "name": "workers-kue",
  "version": "1.0.0",
  "description": "A KueJS implementation for common async tasks (written in Typescript)",
  "main": "index.ts",
  "scripts": {
    "dev": "concurrently \"npx tsc -w\" \"nodemon ./build/src/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "start": "node ./build/src/index.js",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "@kiwicopple",
  "license": "ISC",
  "devDependencies": {
    "@types/dotenv": "^4.0.3",
    "@types/node": "^10.5.6",
    "concurrently": "^3.6.1",
    "gts": "^0.8.0",
    "nodemon": "^1.18.3",
    "typescript": "~2.8.0"
  },
  "dependencies": {
    "@types/express": "^4.16.0",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "kue": "^0.11.6"
  }
}
